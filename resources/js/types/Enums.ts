export enum AppointmentStatus {
    PENDING = 'pending',
    SCHEDULED = 'scheduled',
    CANCELED = 'canceled',
    COMPLETED = 'completed',
}

export enum DashboardAction {
    CONFIRM = 'confirm',
    CANCEL = 'cancel',
}
